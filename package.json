{
  "name": "multiview",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted Holodex-style multiview: track creators across YouTube, Twitch and Kick, and watch several at once.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
