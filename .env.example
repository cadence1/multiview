# Copy this file to .env and fill in what you have. Nothing here is required
# to run the app — YouTube works out of the box via page-scraping — but
# adding keys makes status checks more reliable, and is required for Twitch
# and Kick.

# Port the server (and, in Docker, the whole app) listens on.
PORT=8080

# Optional. Google Cloud Console -> APIs & Services -> Credentials -> API key,
# with the "YouTube Data API v3" enabled. Improves channel lookup reliability;
# live/upcoming detection itself never uses API quota.
YOUTUBE_API_KEY=

# Required for Twitch support. Free app at https://dev.twitch.tv/console/apps
# (any redirect URL works, e.g. http://localhost — it's not used).
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=

# Required for Kick support. Free app at https://kick.com/settings/developer
# — any redirect URL works (e.g. http://localhost — it's not used, only the
# client credentials flow is). Needs the "channel:read" and "user:read"
# scopes.
KICK_CLIENT_ID=
KICK_CLIENT_SECRET=

# How often (in seconds) the server re-checks live/upcoming status for every
# tracked creator. Separately, any creator already showing "upcoming" with a
# scheduled start within a few minutes (in either direction) gets rechecked
# every 60s regardless of this setting, so a stream that's about to start
# doesn't sit undetected for the whole interval.
POLL_INTERVAL_SECONDS=300

# Recording (optional feature — see README's "Recording" section). Requires
# yt-dlp and ffmpeg on PATH (both already in the Docker image); nothing
# breaks without them, recording attempts just fail with a clear error.

# Where recordings are stored. Defaults to DATA_DIR/recordings if unset —
# only set this if you want them somewhere else (e.g. a mounted SMB share;
# an already-mounted network path works here with no other changes needed).
RECORDINGS_DIR=

# Refuses to *start* a new recording below this much free disk space, and
# also stops every currently-active recording if free space drops below it
# mid-recording — as a safety net against ever filling the disk completely,
# not a retention policy (there isn't one; recordings are only ever removed
# by an explicit delete in the UI). 0 disables both checks.
RECORDING_MIN_FREE_GB=5

# Live recordings only (manual or auto-record) — a request past this many
# simultaneous recordings is rejected outright, not queued (queueing
# something time-sensitive just means missing the part spent waiting).
RECORDING_MAX_CONCURRENT=4

# Optional — offload finished recordings to an S3-compatible object store
# (AWS S3, self-hosted MinIO, Cloudflare R2, Backblaze B2, etc.) instead of
# keeping them on local disk. Recording itself always happens to local disk
# first either way (a live stream can't be written straight into an object
# store) — this only affects what happens once a recording finishes: on a
# successful upload, the local copy is deleted and play/download in the UI
# is proxied through the server from the bucket instead. Leave all of these
# blank to keep everything local (the default) — set BUCKET plus both
# credentials to turn it on.
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# Leave blank to talk to real AWS S3. Set this for MinIO/R2/B2/etc, e.g.
# http://minio.local:9000 for a self-hosted MinIO instance.
S3_ENDPOINT=
S3_REGION=us-east-1
# Optional "subfolder" within the bucket — object keys become
# `${S3_KEY_PREFIX}<filename>`.
S3_KEY_PREFIX=
# Path-style addressing (bucket name in the URL path, not a subdomain) —
# what self-hosted S3-compatible servers generally need. Defaults to on
# whenever S3_ENDPOINT is set (i.e. probably not real AWS) and off
# otherwise; set explicitly to override either way.
S3_FORCE_PATH_STYLE=
